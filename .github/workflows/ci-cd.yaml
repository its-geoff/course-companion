name: CI/CD Pipeline

on:
  pull_request:
    branches: [ main, develop ]
    types: [ opened, synchronize ]

  push:
    branches: [ main, develop ]
    tags:
      - 'v*'

  workflow_dispatch:

  schedule:
    - cron: '0 8 * * *'

jobs:
  test-client:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.10.1'
          host: 'linux'
          target: 'desktop'
          cache: true

      - name: Install system dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y uuid-dev build-essential python3-pip
          pip install conan --break-system-packages
          conan profile detect --force

      - name: Cache Conan packages
        uses: actions/cache@v5
        with:
          path: ~/.conan2
          key: ${{ runner.os }}-conan-${{ hashFiles('client/conan.lock') }}
          restore-keys: |
            ${{ runner.os }}-conan-

      - name: Install Conan dependencies
        working-directory: client
        run: |
          mkdir -p build/build_test
          conan install . --lockfile=conan.lock --build=missing -of build/build_test -s build_type=Debug

      - name: Configure CMake (Test)
        working-directory: client
        run: |
          cmake -S . -B build/build_test \
            -DCMAKE_BUILD_TYPE=Debug \
            -DBUILD_TESTING=ON \
            -DCMAKE_TOOLCHAIN_FILE=build/build_test/conan_toolchain.cmake \
            -DCMAKE_PREFIX_PATH="${Qt6_DIR}"

      - name: Build tests
        working-directory: client
        run: cmake --build build/build_test

      - name: Run tests
        working-directory: client
        run: ctest --test-dir build/build_test --output-on-failure

  test-server:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      DB_PASSWORD: ${{ secrets.DB_PASSWORD || 'test_password' }}
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: ${{ env.DB_PASSWORD }}
          MYSQL_DATABASE: course_companion_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install dependencies
        run: uv sync

      - name: Initialize test database
        env:
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_USER: root
          DB_PASSWORD: ${{ env.DB_PASSWORD }}
          TEST_DB_SCHEMA: course_companion_test
        run: uv run python scripts/init-db.py

      - name: Format check (black)
        run: uv run black --check .

      - name: Import sort check (isort)
        run: uv run isort --check .

      - name: Lint (flake8)
        run: uv run flake8 .

      - name: Run tests
        env:
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_USER: root
          DB_PASSWORD: ${{ env.DB_PASSWORD }}
          TEST_DB_SCHEMA: course_companion_test
        run: uv run pytest

  docker:
    needs: [ test-client, test-server ]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/'))
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}

      - name: Build and push client image
        uses: docker/build-push-action@v7
        with:
          context: ./client
          push: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/')) }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  release:
    if: github.ref_type == 'tag' && github.ref_name != ''
    needs: [ docker ]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Create GitHub Release
        uses: elgohr/Github-Release-Action@v5
        with:
          title: Release ${{ github.ref_name }}
          tag: ${{ github.ref_name }}
          body: |
            This release is distributed via Docker images published to GitHub Container Registry (GHCR).

            Pull the image for this release using:
            ```bash
            docker pull ghcr.io/${{ github.repository }}:${{ github.ref_name }}
            ```

            For additional tags and images, see: https://ghcr.io/${{ github.repository }}