#!/bin/bash

usage() {
    echo "Usage: clear-build [-td]"
    echo "  -t: Clear build_test files"
    echo "  -t: Clear build_test_db files"
    exit 1
}

option_given=0

while getopts ":td" opt; do
    option_given=1

    case $opt in
        t)
            rm -rf build_test/*
            ;;
        d)
            rm -rf build_test/*
            ;;
        \?)
            echo "Invalid option: -$opt" >&2
            usage
            ;;
    esac
done

if [ $option_given -eq 0 ]; then
    rm -rf build_main/*
fi